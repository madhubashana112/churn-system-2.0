[pytest]
# The repo has no __init__.py anywhere and relies on PEP 420 namespace packages,
# so put the repo root on sys.path rather than adding package markers.
pythonpath = .
testpaths = tests
addopts = -ra
filterwarnings =
    error::UserWarning
